#! /bin/bash
echo "Hello world"
# comments are used to write invisible texts or codes.
# variables are names that have values assigned to them. there are two types of variables : system defined variables-which are variables that are defined by the computer. it is defined using capital letters. eg PWD, BASH, BASH_VERSION, etc. the user defined variables are variables that bare defined the users. eg name, age, school, etc. to declare a variable, the dollar sign is used and it comes first before the variable

#EXAMPLE OF SYSTEM DEFINED VARIABLE IS PWD

echo "your current location is: $PWD"

#EXAMPLE OF USER DEFINED VARIABLES IS name

name="i-am-star"
echo "This user is called $name"
